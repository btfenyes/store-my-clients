Two run the project, the server and the client should be started in two separate terminal windows.

Server:
  ```
  cd server
  npm i
  npm run dev
  ```
  
Client
  ```
  cd client
  npm i
  npm run start
  ```
